project
- system design simulation
- user can add components (user traffic, load balancer,server, db,etc) {1webpage}
 -- user have to login in the beginning
 -- after login the user will see a carousel of components desc like wht is a load balancer
 -- user can undo the steps until it is pushed
 -- multi user (like github), stores commit history at backend
 -- the designs of the user will be stored at the backend
 -- on clicking on any component we can see its health and other desc
 -- there will be a button, by clicking on it we can see the time at which each component send or recieve req as at t=0 the req begins from the user app
 -- after every collapse the system will suggest the fix for last collapses everytime you desing system
 -- there will be also few templates of system design
 -- if user add any component it will show the estimated cost
 -- there will be diff around 2-3 models of each component
 -- components will be connected by links ui and can be drag drop
 -- we can control the user traffic
 -- dark mode/light mode both
 -- chat bot : the user will present his idea to the bot then it will design the system acc to him
 -- main idea: when no of users become high so the system will collapse and an alert wil be shown then by clicking on a button the system will redesign itsellf by adding more components to handle the load
- how to prevent system from hacking {2webpage}
 -- all features of the webpage 1 , and user can the anti hacking components
- how to implement ai workflow for aautomation
 -- 
 -- there will be animations